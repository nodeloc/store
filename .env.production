# ╔════════════════════════════════════════════════════════════╗
# ║       NodeLoc 社区发卡系统 - 生产环境配置示例                ║
# ╚════════════════════════════════════════════════════════════╝
# 使用 Docker Compose 一键部署时使用此配置

# ===========================================
# 数据库配置（Docker 内置 MySQL）
# ===========================================
# MySQL Root 密码（请务必修改）
DB_ROOT_PASSWORD=your_strong_root_password_here

# 应用数据库配置
DB_NAME=faka
DB_USER=faka
DB_PASSWORD=your_strong_db_password_here
DB_PORT=3306

# ===========================================
# 服务器配置
# ===========================================
# 前端访问端口
PORT=3000

# Session 密钥（建议使用随机字符串）
SESSION_SECRET=your_random_session_secret_here_32_chars

# Gin 运行模式
GIN_MODE=release

# ===========================================
# Docker 镜像配置
# ===========================================
# Docker Hub 镜像（默认使用 NodeLoc 官方镜像）
DOCKER_IMAGE_BACKEND=nodeloc/faka-backend:latest
DOCKER_IMAGE_FRONTEND=nodeloc/faka-frontend:latest

# ===========================================
# NodeLoc OAuth 配置（必填）
# ===========================================
NODELOC_URL=https://www.nodeloc.com

# OAuth 应用凭证
# 从 https://www.nodeloc.com/oauth-provider/applications 获取
NODELOC_CLIENT_ID=your-oauth-client-id
NODELOC_CLIENT_SECRET=your-oauth-client-secret

# OAuth 回调地址（必须与 NodeLoc 应用设置一致）
# 格式：https://your-domain.com/auth/callback
NODELOC_REDIRECT_URI=https://your-domain.com/auth/callback

# ===========================================
# NodeLoc Payment 配置（可选）
# ===========================================
# 从 https://www.nodeloc.com/payment/applications 获取
# 如不配置，系统将使用免费模式（用户下单直接获得卡密）
PAYMENT_ID=
PAYMENT_SECRET=

# 支付回调地址
# 格式：https://your-domain.com/payment/callback
PAYMENT_CALLBACK_URI=
